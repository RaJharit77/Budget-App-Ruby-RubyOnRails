<!DOCTYPE html>
<html lang="en">
<head>
  <title><%= content_for(:title) || "Budget App" %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="mobile-web-app-capable" content="yes">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= yield :head %>

  <link rel="icon" href="/icon.jpg" type="image/svg+xml">
  <link rel="apple-touch-icon" href="/icon.jpg" type="image/svg+xml">

  <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
  <%= javascript_include_tag "chartkick" %>
  <%= javascript_include_tag "Chart.bundle" %>
</head>

<body class="bg-opacity-50">
<main class="container mx-auto px-5 flex flex-col items-center justify-center min-h-screen">
  <%= yield %>
</main>
</body>
</html>